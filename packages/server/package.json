{
  "name": "@thymeapp/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "precheck": "bun run type-check && bun run lint",
    "smoke": "bun smoke.ts"
  },
  "dependencies": {
    "@thymeapp/logging": "workspace:*",
    "@thymeapp/types": "workspace:*",
    "elysia": "^1.4.29"
  }
}
