{
  "name": "@thymeapp/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "type-check": "tsc --noEmit",
    "test": "jest --config ../../jest.config.js --selectProjects core",
    "precheck": "bun run type-check && bun run lint"
  },
  "dependencies": {
    "@thymeapp/types": "workspace:*"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.1"
  }
}
