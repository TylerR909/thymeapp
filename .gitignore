# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv — commit `.env.example`, not `.env`.
.env
.env.local
.env.*.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Expo
.expo
dist
web-build
expo-env.d.ts
# CNG native trees. Belong under packages/mobile (already ignored there).
# Also ignore the repo root — `expo prebuild` from here dumps ios/ android/ at /.
/ios
/android

# Conductor machine-local overrides
.conductor/settings.local.toml
