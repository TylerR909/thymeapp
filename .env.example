# Copy to `.env` (gitignored). Compose loads `.env` for YAML interpolation
# and injects it into the server via env_file.
STAGE=local
DB_USER=thymeapp
DB_PASSWORD=thymeapp
DB_DATABASE=thymeapp
LOG_LEVEL=DEBUG
LOG_FORMAT=pretty
TEST_PARALLELISM=1
